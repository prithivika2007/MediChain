// Settings the frontend AND the backend must agree on.
export const EXPIRY_WARNING_DAYS = 30;   // warn when a pack expires within this many days
export const MAX_PACKS_PER_BATCH = 500;  // most packs one batch may have
export const DEFAULT_LOCATION = 'Chennai';
